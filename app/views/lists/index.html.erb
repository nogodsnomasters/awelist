<h1>Listing lists</h1>

<table>
  <tbody>
    <% @lists.each do |list| %>
      <tr>
        <td><%= link_to list.title , list %></td>
        <td><%= link_to 'Edit', edit_list_path(list) %></td>
        <td><%= link_to 'Destroy', list, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		<td><%= link_to '↑', move_up_list_path(list), method: :patch, style: 'text-decoration: none;' %>
		<td><%= link_to '↓', move_down_list_path(list), method: :patch, style: 'text-decoration: none;' %>
			
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New List', new_list_path %>
